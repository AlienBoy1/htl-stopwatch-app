/**
 * storage.ts
 *
 * Propósito: Centralizar claves de almacenamiento local de la PWA.
 *
 * Responsabilidades:
 * - Definir identificadores únicos para datos persistidos en localStorage.
 *
 * Rol en la arquitectura: Constantes compartidas de persistencia cliente.
 */

/** Clave de localStorage para el listado de sesiones guardadas. */
export const SAVED_SESSIONS_STORAGE_KEY = 'htl-stopwatch-saved-sessions';
